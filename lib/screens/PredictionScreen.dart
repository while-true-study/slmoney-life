import 'package:flutter/material.dart';

class PredictionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('소비 예측 페이지 (추후 구현)'),
    );
  }
}
